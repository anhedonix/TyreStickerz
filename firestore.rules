rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Check User Role
    function userIsDev() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data['type'] == "DEV";
    }
    function userIsAdmin() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data['type'] in ["ADMIN", "DEV"];
    }

    match /_meta/{MetaID}{
      allow read;
      allow write: if userIsAdmin();
    }

    match /users/{UserID} {
      allow read;
      allow write: if userIsAdmin() || request.auth.uid == UserID;
    }

    match /StickerGraphics/{StickerID} {
      allow read;
      allow write: if userIsAdmin();
    }

    match /Environments/{EnvID} {
      allow read;
      allow write: if userIsAdmin();
    }

    match /Rims/{RimID} {
      allow read;
      allow write: if userIsAdmin();
    }

    match /Wheels/{WheelID} {
      allow read;
      allow write: if userIsAdmin();
    }

    match /Accessories/{AccessoryID} {
      allow read;
      allow write: if userIsAdmin();
    }
  }
}
